meta {
  name: Проверка действительности токена
  type: http
  seq: 2
}

post {
  url: {{URL}}/api/v1/auth/token/verify/
  body: json
  auth: inherit
}

body:json {
  {
    "token": "{{accessToken}}"
  }
}

settings {
  encodeUrl: true
}
