meta {
  name: Отправка SMS кода
  type: http
  seq: 1
}

post {
  url: {{URL}}/api/v1/auth/sms/
  body: json
  auth: inherit
}

body:json {
  {
    "phone": "{{phone_mk}}"
  }
}

settings {
  encodeUrl: true
}
