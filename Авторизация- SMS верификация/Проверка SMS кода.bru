meta {
  name: Проверка SMS кода
  type: http
  seq: 2
}

put {
  url: {{URL}}/api/v1/auth/sms/
  body: json
  auth: inherit
}

body:json {
  {
    "phone": "{{phone_mk}}",
    "code": "1129"
  }
}

tests {
  // Данные ответа уже объект
  const data = res.body;
  
  // Сохраняем переменные
  bru.setVar("accessToken", data.access);
  
}

settings {
  encodeUrl: true
}
